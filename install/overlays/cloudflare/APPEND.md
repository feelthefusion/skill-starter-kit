
## Works with → (Skill Starter Kit)
- **`cloud-clis`** — which CLI does what across GitHub / Railway / Cloudflare / AWS / Google Cloud, auth checks, and the kit's CLI-first rule (MCP servers are opt-in).
- **`verify-gate`** — `./verify.sh` passes before any deploy; **`guardrails`** blocks irreversible cloud deletes.
